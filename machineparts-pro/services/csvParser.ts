// This service is deprecated. Data is now handled by medusaService.ts
import { Machine } from '../types';

export const parseCSV = (csvContent: string, isDetailed: boolean = false): Machine[] => {
    return [];
};
