# Multi-Carrier Shipping System - Architecture

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (Next.js 15)                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────┐  ┌──────────────────┐  ┌───────────────┐ │
│  │ ShippingSelector │  │  TrackingPage    │  │ TrackingTime- │ │
│  │   Component      │  │   Component      │  │ line Component│ │
│  └────────┬─────────┘  └────────┬─────────┘  └───────┬───────┘ │
│           │                     │                     │          │
│  ┌────────▼─────────┐  ┌───────▼──────────┐         │          │
│  │  useShipping()   │  │  useTracking()   │◄────────┘          │
│  │      Hook        │  │      Hook        │                     │
│  └────────┬─────────┘  └───────┬──────────┘                     │
│           │                     │                                │
└───────────┼─────────────────────┼────────────────────────────────┘
            │                     │
            │ HTTP/REST           │ HTTP/REST
            │                     │
┌───────────▼─────────────────────▼────────────────────────────────┐
│                      BACKEND (Medusa 2.0)                         │
├───────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                      API ROUTES                              │ │
│  ├─────────────────────────────────────────────────────────────┤ │
│  │  Store:                        Admin:                        │ │
│  │  • GET  /shipping/methods      • POST /orders/:id/shipment  │ │
│  │  • POST /shipping/rates        • GET  /shipments/:id/label  │ │
│  │  • GET  /shipments/:id/track                                │ │
│  │                                                              │ │
│  │  Webhooks:                                                   │ │
│  │  • POST /webhooks/inpost                                    │ │
│  │  • POST /webhooks/dpd                                       │ │
│  │  • POST /webhooks/dhl                                       │ │
│  └────────────────────────┬─────────────────────────────────────┘ │
│                           │                                        │
│  ┌────────────────────────▼─────────────────────────────────────┐ │
│  │                  ShippingService (Router)                     │ │
│  │  • Provider selection logic                                   │ │
│  │  • Rate aggregation                                          │ │
│  │  • Error handling                                            │ │
│  └────────────────────────┬─────────────────────────────────────┘ │
│                           │                                        │
│         ┌─────────────────┼─────────────────┐                     │
│         │                 │                 │                     │
│  ┌──────▼──────┐  ┌──────▼──────┐  ┌──────▼──────┐              │
│  │   InPost    │  │     DPD     │  │     DHL     │              │
│  │  Provider   │  │  Provider   │  │  Provider   │              │
│  │             │  │             │  │             │              │
│  │ • Poland    │  │ • Europe    │  │ • Global    │              │
│  │ • 3 methods │  │ • 2 methods │  │ • 2 methods │              │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘              │
│         │                 │                 │                     │
└─────────┼─────────────────┼─────────────────┼─────────────────────┘
          │                 │                 │
          │ HTTPS           │ HTTPS           │ HTTPS
          │                 │                 │
┌─────────▼─────────────────▼─────────────────▼─────────────────────┐
│                    EXTERNAL PROVIDER APIs                          │
├────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────┐  ┌──────────────────┐  ┌─────────────────┐ │
│  │  InPost API      │  │    DPD API       │  │    DHL API      │ │
│  │  api-shipx-pl    │  │  dpd.com.pl/api  │  │  api.dhl.com    │ │
│  │  .easypack24.net │  │                  │  │                 │ │
│  └──────────────────┘  └──────────────────┘  └─────────────────┘ │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                      DATABASE (PostgreSQL)                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  shipment                                                     │  │
│  │  ├─ id (UUID)                                                │  │
│  │  ├─ order_id (FK → order)                                    │  │
│  │  ├─ provider (inpost/dpd/dhl)                                │  │
│  │  ├─ tracking_number                                          │  │
│  │  ├─ label_url                                                │  │
│  │  ├─ status                                                   │  │
│  │  └─ metadata (JSONB)                                         │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  tracking_event                                              │  │
│  │  ├─ id (UUID)                                                │  │
│  │  ├─ shipment_id (FK → shipment)                             │  │
│  │  ├─ status                                                   │  │
│  │  ├─ location                                                 │  │
│  │  ├─ timestamp                                                │  │
│  │  └─ description                                              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### 1. Rate Calculation Flow
```
Customer enters address
    ↓
Frontend: useShipping.fetchRates()
    ↓
POST /store/shipping/rates
    ↓
ShippingService.getRates()
    ↓
Query all providers in parallel
    ↓
Aggregate & sort by price
    ↓
Return rates to frontend
    ↓
Display in ShippingSelector
```

### 2. Shipment Creation Flow
```
Admin confirms order
    ↓
POST /admin/orders/:id/shipment
    ↓
ShippingService.createShipment()
    ↓
Select provider based on destination
    ↓
Call provider API
    ↓
Save to database (shipment table)
    ↓
Return tracking number & label URL
```

### 3. Tracking Flow
```
Customer views order
    ↓
Frontend: useTracking(trackingNumber)
    ↓
GET /store/shipments/:id/tracking
    ↓
ShippingService.track()
    ↓
Call provider tracking API
    ↓
Update database (tracking_event table)
    ↓
Return events to frontend
    ↓
Display in TrackingTimeline
    ↓
Auto-refresh every 60s
```

### 4. Webhook Flow
```
Provider sends update
    ↓
POST /webhooks/{provider}
    ↓
Validate webhook
    ↓
Find shipment by tracking_number
    ↓
Update shipment status
    ↓
Create tracking_event
    ↓
Return success
```

## 🎯 Provider Selection Logic

```
┌─────────────────────────────────────────┐
│  Destination Country                     │
└──────────────┬───────────────────────────┘
               │
       ┌───────┴───────┐
       │               │
   Poland?         EU Country?
       │               │
      Yes             Yes
       │               │
       ▼               ▼
   ┌───────┐      ┌───────┐
   │InPost │      │  DPD  │
   └───────┘      └───────┘
       │               │
       No              No
       │               │
       └───────┬───────┘
               │
               ▼
          ┌───────┐
          │  DHL  │
          └───────┘
         (Global)
```

## 📊 Rate Calculation

```
Base Rate
    +
Weight Surcharge (if > 5kg)
    +
Insurance (optional)
    +
Saturday Delivery (optional)
    =
Final Rate
```

**Example:**
```
InPost Paczkomat: $4.99
Weight: 7kg → +$2.00 (2kg over limit)
Insurance: $500 → +$2.50
─────────────────────────
Total: $9.49
```

## 🔐 Security Layers

```
┌─────────────────────────────────────────┐
│  1. API Key Authentication              │
│     • Provider credentials in .env      │
│     • Secure storage                    │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  2. Input Validation                    │
│     • Required fields check             │
│     • Type validation                   │
│     • Sanitization                      │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  3. Error Handling                      │
│     • Try-catch blocks                  │
│     • Sanitized error messages          │
│     • Logging                           │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  4. Database Security                   │
│     • Parameterized queries             │
│     • Foreign keys                      │
│     • Indexes                           │
└─────────────────────────────────────────┘
```

## 🚀 Performance Optimization

### Database Indexes
```sql
CREATE INDEX idx_shipment_order_id ON shipment(order_id);
CREATE INDEX idx_shipment_tracking_number ON shipment(tracking_number);
CREATE INDEX idx_shipment_status ON shipment(status);
CREATE INDEX idx_tracking_event_shipment_id ON tracking_event(shipment_id);
```

### Parallel Rate Fetching
```typescript
// Query all providers simultaneously
const ratePromises = providers.map(provider => 
  provider.calculateRate(origin, destination, parcel)
);
const allRates = await Promise.all(ratePromises);
```

### Frontend Optimization
- Auto-refresh with 60s interval (not too frequent)
- Loading states for better UX
- Error boundaries
- Memoized components

## 📈 Scalability

### Horizontal Scaling
- Stateless API design
- Database connection pooling
- Provider API rate limiting handled

### Caching Strategy
```
Rate Cache: 5 minutes
  • Rates don't change frequently
  • Reduces provider API calls

Tracking Cache: 1 minute
  • Balance between freshness and load
  • Auto-refresh on frontend
```

## 🔧 Maintenance Points

### Regular Monitoring
- Provider API response times
- Webhook delivery success rate
- Shipment creation success rate
- Tracking update frequency

### Error Scenarios
```
Provider API Down
    ↓
Fallback to cached rates
    ↓
Show warning to user
    ↓
Log for admin review

Webhook Failed
    ↓
Provider retries (their side)
    ↓
Manual sync available
    ↓
Admin notification
```

## 🎯 Key Design Decisions

1. **Abstract Base Class**
   - Shared functionality across providers
   - Easy to add new providers
   - Consistent interface

2. **Service Manager Pattern**
   - Centralized provider routing
   - Single entry point
   - Easy testing

3. **Webhook Architecture**
   - Real-time updates
   - Reduced polling
   - Better performance

4. **JSONB Metadata**
   - Flexible provider-specific data
   - No schema changes needed
   - Easy to extend

5. **Frontend Hooks**
   - Reusable logic
   - Clean separation
   - Easy testing

## 📚 Technology Stack

```
Frontend:
├── Next.js 15
├── React 18
├── TypeScript
└── Custom Hooks

Backend:
├── Medusa 2.0
├── TypeScript
├── PostgreSQL
└── TypeORM

Providers:
├── InPost API
├── DPD API
└── DHL API
```

## 🎉 Summary

This architecture provides:
- ✅ Scalability
- ✅ Maintainability
- ✅ Extensibility
- ✅ Performance
- ✅ Security
- ✅ Reliability

Ready for production deployment! 🚀
